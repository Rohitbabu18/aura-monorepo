model Hospital {
  id                   String            @id @default(uuid())
  name                 String
  email                String            @unique
  phone                String?
  alternatePhone       String?
  role                 HospitalRole      @default(HOSPITAL)
  facilityType         String?
  ownershipType        String?
  numberOfBeds         Int?
  numberOfIcuBeds      Int?
  numberOfVentilators  Int?
  totalStaff           Int?
  emergencyAvailable   Boolean?
  ambulanceAvailable   Boolean?
  specialtiesOffered   String[]
  accreditationDetails String?
  registrationNumber   String?
  yearEstablished      Int?
  licenseNumber        String?
  servicesOffered      String[]
  websiteUrl           String?
  about                String?
  coverImageUrl        String?
  photoUrls            String[]
  ambulancePhone       String?
  isActive             Boolean           @default(true)
  ratingAvg            Float             @default(0)
  ratingCount          Int               @default(0)
  createdAt            DateTime          @default(now())
  updatedAt            DateTime          @updatedAt
  address              Address?
  complaints           Complaint[]
  bookings             HospitalBooking[]
  packages             HospitalPackage[]
  rooms                HospitalRoom[]
  operatingData        OperatingData?
  reviews              Review[]
  doctors              Doctor[]          @relation("DoctorToHospital")
}

model OperatingData {
  id               String   @id @default(uuid())
  hospitalId       String   @unique
  mondayEnabled    Boolean?
  mondayStart      String?
  mondayEnd        String?
  tuesdayEnabled   Boolean?
  tuesdayStart     String?
  tuesdayEnd       String?
  wednesdayEnabled Boolean?
  wednesdayStart   String?
  wednesdayEnd     String?
  thursdayEnabled  Boolean?
  thursdayStart    String?
  thursdayEnd      String?
  fridayEnabled    Boolean?
  fridayStart      String?
  fridayEnd        String?
  saturdayEnabled  Boolean?
  saturdayStart    String?
  saturdayEnd      String?
  sundayEnabled    Boolean?
  sundayStart      String?
  sundayEnd        String?
  hospital         Hospital @relation(fields: [hospitalId], references: [id])
}

model HospitalRoom {
  id                   String            @id @default(uuid())
  hospitalId           String
  roomType             RoomType
  name                 String
  feePerDay            Int
  patientsPerRoom      Int               @default(1)
  serviceChargePercent Int?
  doctorVisitCharge    Int?
  minBookingAmount     Int?
  photoUrls            String[]
  isAvailable          Boolean           @default(true)
  bookings             HospitalBooking[]
  hospital             Hospital          @relation(fields: [hospitalId], references: [id], onDelete: Cascade)

  @@index([hospitalId, roomType])
}

model HospitalPackage {
  id          String   @id @default(uuid())
  hospitalId  String
  name        String
  description String?
  price       Int
  hospital    Hospital @relation(fields: [hospitalId], references: [id], onDelete: Cascade)

  @@index([hospitalId])
}
