model User {
  id        String   @id @default(uuid())
  name      String?
  email     String   @unique
  password  String
  address                Address?
  location               Location?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
